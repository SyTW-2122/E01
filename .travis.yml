language: node_js
node_js:
  - "16"

addons:
  chrome: stable

cache:
  directories:
    - ./node_modules
    
install:
  - npm install ./server
  - npm install

script:
  - mongosh
  - cd server
  - npm start
  - cd ..
  - npm run test -- --no-watch --no-progress --browsers=ChromeHeadless